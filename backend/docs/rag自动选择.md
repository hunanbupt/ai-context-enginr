现在 RAG 主流程已经可以端到端跑通，但当前使用方式仍然需要用户手动传 ragEnabled=true 和固定 kbId。 我现在希望优化为：用户不需要固定选择 kbId，系统可以根据文章主题、标题或大纲内容，自动判断是否有合适的 RAG 知识库。如果有相似度足够高的知识库，就自动调用 RAG；如果没有合适知识库，就走普通生成。我想你给我一个改进.md文件
请实现第八阶段：RAG 自动知识库选择与调用决策。 注意： 当前项目使用 MyBatis-Flex，不是 MyBatis-Plus。 当前检索使用的是 EmbeddingUtils 的文本相似度，不是真实向量数据库。 本阶段不要引入真实向量数据库。 不要修改 SSE。 不要修改配图流程。 不要破坏普通文章生成流程。 一、新增 RagModeEnum 请新增枚举 RagModeEnum，包含： OFF：不使用 RAG ON：强制使用指定 kbId AUTO：自动选择知识库并决定是否调用 RAG 要求： - 保持项目已有枚举风格； - 包含 value、description； - 提供 getByValue 方法。 二、修改 ArticleCreateRequest 新增字段： private String ragMode; 说明： - ragMode 可以为空； - 为空时默认 OFF，保持普通生成不受影响； - ragMode=ON 时必须传 kbId； - ragMode=AUTO 时可以不传 kbId，由系统自动选择知识库。 保留原来的 ragEnabled 和 kbId，不要删除，避免影响已有代码。 兼容规则： 1. 如果 ragMode 为空，但 ragEnabled=true，则视为 ON； 2. 如果 ragMode 为空，ragEnabled 不为 true，则视为 OFF； 3. 如果 ragMode=AUTO，则不要求传 kbId。 三、修改 Article 实体和 article 表 请给 article 表新增字段： ragMode VARCHAR(16) DEFAULT 'OFF' Article 实体新增： private String ragMode; 要求： - 保持 MyBatis-Flex 风格； - ragEnabled 仍然保留； - kbId 仍然保留； - AUTO 模式下，kbId 可以为空，后续由系统选择后写入 ArticleState，不一定写回 Article 表； - 如果你认为有必要，也可以在选中知识库后更新 article.kbId，但不要影响生成流程。 四、新增 KnowledgeBaseRouterService 请新增： service/KnowledgeBaseRouterService.java service/impl/KnowledgeBaseRouterServiceImpl.java 作用： 根据 query 自动从当前用户的所有知识库中选择最相关的知识库。 建议方法： KnowledgeBaseRouteResult route(Long userId, String query, Integer topK); 五、新增 KnowledgeBaseRouteResult 请新增： rag/KnowledgeBaseRouteResult.java 字段包括： private Boolean matched; private String kbId; private String kbName; private Double score; private String reason; private List<RetrievedChunk> chunks; 字段说明： - matched：是否找到合适知识库； - kbId：最相关知识库 ID； - kbName：知识库名称； - score：该知识库最高或平均相关度； - reason：路由原因； - chunks：该知识库下检索出的 topK 切片。 六、KnowledgeBaseRouterService 实现逻辑 route(userId, query, topK) 的逻辑： 1. 校验 userId 不为空； 2. 校验 query 不为空； 3. 查询当前用户所有未删除、状态正常的知识库； 4. 如果没有知识库，返回 matched=false，reason=用户无可用知识库； 5. 遍历每个知识库： - 查询该知识库下的 course_document_chunk； - 如果没有切片，跳过； - 调用现有 RagService.retrieveByUserId(kbId, query, topK, userId)； - 取该知识库 topK chunks 的最高 score 或平均 score； 6. 选择 score 最高的知识库； 7. 设置阈值，例如 minScore=0.25； 8. 如果最高 score < minScore，返回 matched=false，reason=未找到足够相关的知识库； 9. 如果 score 达标，返回 matched=true、kbId、kbName、score、chunks。 注意： - 第一版使用现有 EmbeddingUtils 文本相似度即可； - 不要引入真实 embedding； - 日志打印每个知识库的 score，方便调试。 七、修改 RagDecisionService 如果之前已经有 RagDecisionService，请在里面支持 ragMode。 决策逻辑： 1. ragMode=OFF： useRag=false，reason=RAG模式为OFF。 2. ragMode=ON： - 必须有 kbId； - 使用指定 kbId 调用 RAG； - 逻辑与当前固定 kbId 方式一致。 3. ragMode=AUTO： - 不要求 kbId； - 调用 KnowledgeBaseRouterService 根据当前阶段 query 自动选择知识库； - 如果 matched=true，则 useRag=true，并使用 routeResult.kbId； - 如果 matched=false，则 useRag=false，普通生成。 八、不同阶段 query 构造规则 AUTO 模式下，不同阶段使用不同 query： 1. 标题阶段： query = topic 2. 大纲阶段： query = topic + mainTitle + subTitle 3. 正文阶段： query = topic + mainTitle + subTitle + outline 要求： - 过滤 null 和空字符串； - 用换行拼接； - query 最大长度 3000 字符； - topK 默认 5。 九、修改 ArticleAsyncService ArticleAsyncService 中原来调用 RAG 的逻辑需要改为： 1. 调用 RagDecisionService 获取决策结果； 2. 如果 useRag=false： - state.ragContext=""; - 不调用 RagService； - 打印日志； 3. 如果 useRag=true 且 decision 中有 kbId： - 调用 RagService.buildRagContextByUserId(decision.kbId, decision.query, decision.topK, article.userId)； - 写入 state.ragContext； - 同时可以 state.setKbId(decision.kbId)，方便后续日志追踪； 4. 如果异常，catch 后降级为空上下文； 5. 不影响普通生成。 十、日志要求 请增加日志，方便判断是否自动选择知识库： 1. RAG 模式判断： taskId、phase、ragMode、useRag、reason。 2. AUTO 路由结果： taskId、phase、queryLength、matched、selectedKbId、score、reason。 3. RAG 上下文注入： taskId、phase、kbId、contextLength。 4. 降级： taskId、phase、reason。 十一、测试要求 测试 1：普通生成 请求： { "topic": "女生回复消息很慢应该怎么办", "style": "education", "ragMode": "OFF" } 预期： - 不调用 RAG； - 文章正常生成。 测试 2：AUTO 模式，有相关知识库 前提： 已创建“恋爱 FAQ 知识库”，并上传了女生回复慢、约会聊天等内容。 请求： { "topic": "女生回复消息很慢应该怎么办", "style": "education", "ragMode": "AUTO" } 预期： - 系统自动扫描当前用户知识库； - 选择恋爱 FAQ 知识库； - 日志显示 selectedKbId 和 score； - 标题、大纲、正文结合恋爱知识库资料。 测试 3：AUTO 模式，无相关知识库 请求： { "topic": "Java 分布式锁怎么实现", "style": "education", "ragMode": "AUTO" } 如果当前用户只有恋爱 FAQ 知识库，预期： - 路由分数低于阈值； - useRag=false； - 普通生成； - 不混入恋爱知识。 测试 4：ON 模式兼容旧逻辑 请求： { "topic": "女生回复消息很慢应该怎么办", "style": "education", "ragMode": "ON", "kbId": "恋爱FAQ知识库kbId" } 预期： - 强制使用指定 kbId； - 和原来 ragEnabled=true + kbId 的效果一致。 十二、本阶段不要做 1. 不要引入真实向量数据库； 2. 不要引入真实 embedding； 3. 不要修改文档上传逻辑； 4. 不要修改 SSE； 5. 不要修改配图流程； 6. 不要删除 ragEnabled； 7. 不要破坏旧的 ragEnabled=true + kbId 调用方式； 8. 不要让 AUTO 模式因为找不到知识库而报错。 十三、最终输出 请完成代码修改，并总结： 1. 新增了哪些文件； 2. 修改了哪些文件； 3. ragMode 如何兼容 ragEnabled； 4. KnowledgeBaseRouterService 如何选择知识库； 5. AUTO 模式如何判断是否调用 RAG； 6. OFF / ON / AUTO 三种模式分别怎么测试； 7. 如何通过日志确认系统自动选择了哪个知识库； 8. 如果没有合适知识库，如何降级普通生成。我现在想让ai改一个自动rag,先生成rag改进v1版本.md